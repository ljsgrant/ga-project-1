function clearRows() {
  for (let index = 296; index > 0; index -= 14) {
    // const rowToCheck = gridSquares.slice(index, index + 10);
    // console.log(rowToCheck);
    console.log(index);
  }
}

clearRows();
